# Backend Environment Variables (for local development and Docker Compose)
# NOTE: For ECS production, use the values in .env.example

# RDS Database Configuration
DB_HOST=localhost
DB_USER=admin
DB_PASSWORD=Admin123
DB_NAME=myappdb

# Server Configuration
PORT=5000
NODE_ENV=production

# CORS Configuration (ALB URL only, no path)
CORS_ORIGIN=http://localhost:3000