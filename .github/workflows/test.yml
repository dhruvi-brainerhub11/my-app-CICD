name: User App
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test job
        run: echo "GitHub CI/CD configured successfully"
